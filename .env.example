# Sanity CMS Configuration
# Get these values from https://sanity.io/manage
NEXT_PUBLIC_SANITY_PROJECT_ID=your-sanity-project-id
NEXT_PUBLIC_SANITY_DATASET=production

# Webhook Security
# Generate a secure secret with: openssl rand -hex 16
# Configure this in Sanity webhook settings (see WEBHOOK_SETUP.md)
REVALIDATE_SECRET=your-webhook-secret-here
